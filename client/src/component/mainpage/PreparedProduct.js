import React, { useState } from 'react';
import styled from 'styled-components';

const PreparedProduct = () => {
  const [preparedItems, setPrepatedItems] = useState(initialState);
  return <div></div>;
};

export default PreparedProduct;
